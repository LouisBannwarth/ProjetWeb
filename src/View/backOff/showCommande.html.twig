{% extends "layout.html.twig" %}
{% block contenu %}

    <div class="small-4 columns">
        <table>
            <caption>affiche table commande</caption>
            <tr>
                <th>id</th>
                <th>user_id</th>
                <th>date ajout</th>
                <th>prix</th>
                <th>etat</th>

            </tr>
            {% if   app.session.get('droit') is defined and  app.session.get('droit') == 'DROITadmin' %}
                {% for data in commande if commande is not empty %}

                    <tr>
                        <td>{{ data.id }}</td>
                        <td>{{ data.user_id }}</td>
                        <td>{{ data.nom }}</td>
                        <td>{{ data.date_achat }}</td>
                        <td>{{ data.prix }}</td>
                        <td>{{ data.libelle }}</td>
                        {% if data.etat_id==1 %}
                            <td><a href="{{ path('produit.editCommande', {id: data.id}) }}">Expedier</a></td>
                        {% endif %}
                    </tr>

                {% endfor %}

            {% endif %}

            <tbody>
        </table>
    </div>


{% endblock %}

